{
	"permissions": {
		"allow": [
			"Bash(npm run check)",
			"Bash(npm run check:watch)",
			"Bash(npm run lint)",
			"Bash(npm run dev)",
			"Bash(npm install)",
			"Bash(npm install *)",
			"Bash(find *)",
			"Bash(curl -s http://localhost:*)",
			"Bash(curl -sf http://localhost:*)",
			"Bash(curl -s -o /dev/null -w * http://localhost:*)"
		]
	}
}
